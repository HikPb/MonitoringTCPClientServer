if [ ! -d "images" ]; then
  # Control will enter here if $DIRECTORY doesn't exist.
	mkdir images
fi
if [ ! -d "info" ]; then
  # Control will enter here if $DIRECTORY doesn't exist.
  mkdir  info 
fi
if [ ! -d "processing" ]; then
  # Control will enter here if $DIRECTORY doesn't exist.
  mkdir processing 
fi
if [ ! -d "result" ]; then
  # Control will enter here if $DIRECTORY doesn't exist.
  mkdir result 
fi
if [ ! -d "logs" ]; then
  # Control will enter here if $DIRECTORY doesn't exist.
  mkdir logs
fi
